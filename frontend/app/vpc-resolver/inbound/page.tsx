"use client";

import { ComingSoonPage } from "@/components/common/ComingSoon";

export default function InboundEndpointsPage() {
  return (
    <ComingSoonPage
      title="Inbound endpoints"
      description="Create inbound endpoints so on-premises DNS resolvers can forward queries to AWS."
    />
  );
}
